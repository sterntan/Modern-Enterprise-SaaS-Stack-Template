# ── Sanity CMS ───────────────────────────────────────────────────────────
# Find these in your sanity.io project dashboard.
# You can generate this file automatically by running `npx sanity init --env`
PUBLIC_SANITY_PROJECT_ID="your-project-id"
PUBLIC_SANITY_DATASET="production"

# (Optional) Read token for viewing draft content locally
# SANITY_API_READ_TOKEN="your-read-token"
