# ── Sanity Studio ────────────────────────────────────────────────────────
# Run `npx sanity init --env` in this directory to auto-generate these values
# from your Sanity.io cloud project.
SANITY_STUDIO_PROJECT_ID="your-project-id"
SANITY_STUDIO_DATASET="production"
